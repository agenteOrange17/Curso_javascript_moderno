/*
- Convierte 20 a string con .toString() y luego a número con parseInt.

*/
const num = '20';
const num2 = parseInt(num);

/*- Explica (en consola) cuándo usas función y cuándo método. */
console.log(num.toString());
console.log(parseInt(num));
